"""Request and response models for the Sukupol Game API.

Typed models enforce the API boundary contract:
- Requests validate input (reject unknown actions, bad formats).
- Responses guarantee the shape of every API response.
"""
